spring.application.name=realestate-api
server.port=${PORT:8080}

springdoc.swagger-ui.path=/swagger-ui.html

# Neon Postgres — reads from env vars, falls back to local values
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-misty-mouse-aotbo1va-pooler.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DATABASE_USERNAME:neondb_owner}
spring.datasource.password=${DATABASE_PASSWORD:npg_ft21QjkrcDTO}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true


# Mail (Brevo HTTP API — SMTP is blocked on Render free tier)
spring.mail.username=${MAIL_USERNAME:nihalgupta375@gmail.com}
brevo.api.key=${BREVO_API_KEY:}
app.notification.email=${NOTIFICATION_EMAIL:nihalgupta375@gmail.com}